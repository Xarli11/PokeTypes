---
// One "Type 1 / Type 2 / Extra Type" field: a visual trigger button (opens
// the shared popover grid from typeSelector.js) plus the real, functional
// <select> it controls — kept in the DOM so every existing main.js
// listener (change events, .value reads/writes from search/reset/URL
// restore) keeps working unchanged. See typeSelector.js for the wiring.
interface Props {
    selectId: string;
    placeholderKey: string;
    extra?: boolean;
    tooltipKey?: string;
}

const { selectId, placeholderKey, extra = false, tooltipKey } = Astro.props;
const helpId = tooltipKey ? `${selectId}-help` : undefined;
---

<div class="relative w-full sm:w-40">
    <button
        type="button"
        class:list={["type-selector-btn", { "type-selector-btn-extra": extra }]}
        data-for={selectId}
        data-placeholder={placeholderKey}
        aria-haspopup="dialog"
        aria-expanded="false"
        aria-describedby={helpId}
    >
        <span class="type-selector-label" data-i18n={placeholderKey}></span>
        <svg class="type-selector-caret w-4 h-4 shrink-0" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
            <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
        </svg>
    </button>

    <select id={selectId} class="sr-only" tabindex="-1" aria-hidden="true">
        <option value="" data-i18n={placeholderKey}></option>
    </select>

    {tooltipKey && (
        <!-- <details> gives us click/tap/keyboard/screen-reader support for
             free (Enter, Space, and tap all toggle <summary>; the expanded
             state is announced natively) — no custom JS needed, and unlike
             the old group-hover-only tooltip this also works on touch. The
             description is additionally wired to the trigger button itself
             via aria-describedby, so screen reader users hear it just by
             focusing the button — they don't have to find/open this too. -->
        <details class="type-selector-help">
            <summary aria-label="Extra Type help">?</summary>
            <div id={helpId} role="tooltip" class="type-selector-help-panel" data-i18n={tooltipKey}></div>
        </details>
    )}
</div>
